#ifndef _FUNCONFIG_H
#define _FUNCONFIG_H

/*
 * Per-app ch32fun configuration. ch32fun.h requires this header to be on
 * the include path; place feature toggles here. The defaults (empty body)
 * are fine for blink — see ch32fun/ch32fun.h for the full list of options.
 */

#endif
