#ifndef _FUNCONFIG_H
#define _FUNCONFIG_H

/*
 * Empty body: ch32fun's defaults for CH32V003 include
 * FUNCONF_USE_DEBUGPRINTF=1, which routes printf() over the SWIO debug
 * channel. minichlink -T picks that up. This matches the upstream
 * ch32fun example that the user confirmed working on this board.
 */

#endif
