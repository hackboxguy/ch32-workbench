#ifndef _FUNCONFIG_H
#define _FUNCONFIG_H

/*
 * Empty body: CH32V003's default FUNCONF_USE_DEBUGPRINTF=1 stays on, so
 * printf() goes over the SWIO debug channel. Capture it with
 * `minichlink -T`. The OLED is the primary display; the SWIO log is a
 * secondary once-per-second debug line.
 */

#endif
